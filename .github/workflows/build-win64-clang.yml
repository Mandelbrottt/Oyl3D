name: Build Win64 Clang

on:
  push:
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  build-win64-clang:
    uses: ./.github/workflows/build-win64-action.yml
    with:
      toolset: clang